global:
  scrape_interval: 1m
  scrape_timeout: 10s

scrape_configs:
  - job_name: xray-exporter
    scrape_interval: 5s
    metrics_path: /scrape
    basic_auth:
      username: prometheus
      password: change-me
    static_configs:
      - targets:
          - xray-exporter:9550
